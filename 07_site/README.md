# Site Workspace

Use this folder for the actual website.

Suggested structure:
- `index.html` for the main entry page
- `pages/` for additional HTML pages
- `styles/` for CSS
- `scripts/` for small JavaScript files
- `assets/` for images, icons, PDFs, and downloads

Keep the code simple. If the site stays static, prefer readable HTML and CSS over introducing a framework.
